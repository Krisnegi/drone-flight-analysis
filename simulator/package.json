{
  "name": "drone-flight-simulator",
  "version": "1.0.0",
  "description": "Standalone drone trajectory and telemetry simulator",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mqtt": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4"
  }
}
